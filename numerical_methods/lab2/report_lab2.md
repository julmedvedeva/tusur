TITLEPAGE

Министерство науки и высшего образования Российской Федерации

Федеральное государственное автономное образовательное учреждение высшего образования

ТОМСКИЙ ГОСУДАРСТВЕННЫЙ УНИВЕРСИТЕТ СИСТЕМ УПРАВЛЕНИЯ И РАДИОЭЛЕКТРОНИКИ (ТУСУР)

Кафедра автоматизированных систем управления (АСУ)

***

# ПРИБЛИЖЕНИЕ ФУНКЦИЙ, ЧИСЛЕННОЕ ДИФФЕРЕНЦИРОВАНИЕ И ИНТЕГРИРОВАНИЕ

Отчёт по лабораторной работе № 2

по дисциплине «Вычислительная математика»

***

Студент гр. ___________

(подпись) Медведева Ю. Е.

(дата)

Руководитель

к.т.н., доц. каф. АСУ

(подпись) В. В. Романенко

оценка ___________

(дата)

***

Томск – 2026

***
END-TITLEPAGE

## ЦЕЛЬ РАБОТЫ

Освоить методы численного приближения функций, дифференцирования и интегрирования. Реализовать на языке Python по одному алгоритму на каждое задание: интерполяцию полиномом Лагранжа с запросом степени полинома у пользователя; численное дифференцирование через производные того же полинома Лагранжа; численное интегрирование по формуле Симпсона с автоматическим выбором шага по правилу Рунге и по формуле Гаусса–Лежандра с самостоятельно вычисляемыми узлами и весами — оба метода вычисляют интеграл с точностью, заданной пользователем.

***

## ОПИСАНИЕ ПРОГРАММНОГО ОБЕСПЕЧЕНИЯ

Программы реализованы на языке Python 3. Используемые стандартные модули:

- `math` — тригонометрические функции, экспонента, константа π.

Внешние библиотеки не используются; все алгоритмы (включая вычисление узлов и весов формулы Гаусса–Лежандра) реализованы вручную. Каждое задание реализовано в отдельном файле: `task_6_3.py`, `task_6_4.py`, `task_6_5.py`. Запуск — командой `python task_6_N.py` из директории `lab2/`; программы запрашивают у пользователя входные параметры (степень полинома, требуемую точность) через `input()` и печатают входные и выходные данные согласно требованиям методических указаний.

***

## ОГЛАВЛЕНИЕ

1. Приближение функций — интерполяция (задание 6.3)
   - 1.1 Задание
   - 1.2 Теоретический материал
   - 1.3 Порядок выполнения работы
   - 1.4 Результаты работы программы
2. Численное дифференцирование (задание 6.4)
   - 2.1 Задание
   - 2.2 Теоретический материал
   - 2.3 Порядок выполнения работы
   - 2.4 Результаты работы программы
3. Численное интегрирование (задание 6.5)
   - 3.1 Задание
   - 3.2 Теоретический материал
   - 3.3 Порядок выполнения работы
   - 3.4 Результаты работы программы
4. Выводы
5. Список использованных источников
6. Приложение А — Листинги программ

***

## 1 ПРИБЛИЖЕНИЕ ФУНКЦИЙ — ИНТЕРПОЛЯЦИЯ

### 1.1 Задание

Написать программу интерполяции таблично заданной функции одним из методов: полиномом Ньютона или полиномом Лагранжа. Выбран **полином Лагранжа**; степень полинома n задаётся пользователем.

**Вариант 10:**

$$y = \sin^2 x + 1, \quad x \in \left[0,\, \frac{\pi}{2}\right], \quad h = \frac{\pi}{20}.$$

- Исходная сетка: $x_i = i \cdot h$, $i = 0, 1, \ldots, 10$ (11 узлов).
- Новая сетка: $x_j = j \cdot \dfrac{h}{2}$, $j = 0, 1, \ldots, 20$ (21 точка).

**Входные данные:** исходная сетка (xᵢ, yᵢ), степень интерполяционного полинома n (вводится пользователем, 1 ≤ n ≤ 10).

**Выходные данные:** новая сетка, значения полинома, погрешность |y_точн − y_полином|.

***

### 1.2 Теоретический материал

**Полином Лагранжа.** Для набора узлов x₀,…,xₙ и значений y₀,…,yₙ интерполяционный полином степени n записывается как

$$P_n(x) = \sum_{i=0}^{n} y_i \cdot L_i(x), \quad L_i(x) = \prod_{\substack{j=0 \\ j \ne i}}^{n} \frac{x - x_j}{x_i - x_j}.$$

Погрешность интерполяции полиномом n-го порядка:

$$R_n(x) = \frac{f^{(n+1)}(\xi)}{(n+1)!} \prod_{i=0}^{n}(x - x_i).$$

Погрешность минимальна, когда точка x находится ближе к центру набора узлов, участвующих в построении полинома. Поэтому при степени n, меньшей числа доступных узлов, для каждой точки x берётся своя группа из n+1 узлов, ближайших к x, а не фиксированный набор.

***

### 1.3 Порядок выполнения работы

1. Запросить у пользователя степень полинома n.
2. Для каждой точки новой сетки xⱼ выбрать n+1 узлов исходной сетки, ближайших к xⱼ (по модулю расстояния).
3. Построить по выбранным узлам полином Лагранжа степени n и вычислить его значение в точке xⱼ.
4. Вычислить погрешность |y(xⱼ) − Pₙ(xⱼ)|.

***

### 1.4 Результаты работы программы

Исходные узлы интерполяции приведены в Таблице 1.1.

**Таблица 1.1 — Исходные узлы интерполяции y = sin²(x) + 1**

| xᵢ | yᵢ = sin²(xᵢ)+1 |
|----|----------------|
| 0.000000 | 1.00000000 |
| 0.157080 | 1.02447174 |
| 0.314159 | 1.09549150 |
| 0.471239 | 1.20610737 |
| 0.628319 | 1.34549150 |
| 0.785398 | 1.50000000 |
| 0.942478 | 1.65450850 |
| 1.099557 | 1.79389263 |
| 1.256637 | 1.90450850 |
| 1.413717 | 1.97552826 |
| 1.570796 | 2.00000000 |

Результаты интерполяции полиномом степени n = 4 (выборка) приведены в Таблице 1.2.

**Таблица 1.2 — Результаты интерполяции полиномом Лагранжа степени n = 4 (выборка)**

| j | xⱼ | y_точн | y_полином | \|err\| |
|---|-----|--------|-----------|--------|
| 0 | 0.000000 | 1.00000000 | 1.00000000 | 0.00e+00 |
| 1 | 0.078540 | 1.00615583 | 1.00613428 | 2.16e-05 |
| 5 | 0.392699 | 1.14644661 | 1.14643582 | 1.08e-05 |
| 9 | 0.706858 | 1.42178277 | 1.42176578 | 1.70e-05 |
| 13 | 1.021018 | 1.72699525 | 1.72697855 | 1.67e-05 |
| 19 | 1.492257 | 1.99384417 | 1.99386572 | 2.16e-05 |
| 20 | 1.570796 | 2.00000000 | 2.00000000 | 0.00e+00 |

Максимальная погрешность при n = 4: **2.16·10⁻⁵**.

*Примечание.* Для каждой точки xⱼ в качестве узлов используются n+1 = 5 ближайших к ней исходных узлов, поэтому точность одинакова во всём диапазоне (нет ухудшения у границ, характерного для полинома с фиксированным направлением обхода узлов).

***

## 2 ЧИСЛЕННОЕ ДИФФЕРЕНЦИРОВАНИЕ

### 2.1 Задание

Написать программу вычисления первой и второй производной табличной функции одним из методов (через полином Ньютона или полином Лагранжа). Выбрано **дифференцирование полинома Лагранжа** — того же метода, что и в задании 6.3, со степенью n, также задаваемой пользователем. Используются функция и сетки из задания 6.3.

**Входные данные:** исходная сетка (xᵢ, yᵢ), степень полинома n (вводится пользователем, 2 ≤ n ≤ 10 — для второй производной нужно не менее трёх узлов).

**Выходные данные:** значения y'(xⱼ), y''(xⱼ), погрешности по сравнению с точными производными.

***

### 2.2 Теоретический материал

Точные производные функции y = sin²x + 1:

$$y'(x) = \sin(2x), \quad y''(x) = 2\cos(2x).$$

**Производная полинома Лагранжа.** Дифференцируя базисные полиномы Lᵢ(x):

$$P'(x) = \sum_{i=0}^n y_i \cdot L_i'(x), \quad L_i'(x) = \sum_{\substack{k=0\\k\ne i}}^n \frac{1}{x_i - x_k} \prod_{\substack{j=0\\j\ne i, j\ne k}}^n \frac{x-x_j}{x_i-x_j}.$$

Вторая производная получается аналогичным дифференцированием Lᵢ'(x) по x.

***

### 2.3 Порядок выполнения работы

1. Запросить у пользователя степень полинома n.
2. Для каждой точки новой сетки xⱼ выбрать n+1 узлов исходной сетки, ближайших к xⱼ.
3. Построить по этим узлам полином Лагранжа и вычислить его первую и вторую производные в точке xⱼ.
4. Вычислить погрешности |y'_точн − P'(x)| и |y''_точн − P''(x)|.

***

### 2.4 Результаты работы программы

Результаты вычисления первой производной y' = sin(2x) при n = 4 (выборка) приведены в Таблице 2.1.

**Таблица 2.1 — Первая производная y' = sin(2x), полином Лагранжа степени n = 4 (выборка)**

| j | xⱼ | y'_точн | y'_полином | \|err\| |
|---|-----|---------|-----------|--------|
| 0 | 0.000000 | 0.000000 | 0.000000 | 1.11e-15 |
| 5 | 0.392699 | 0.707107 | 0.707103 | 4.20e-06 |
| 10 | 0.785398 | 1.000000 | 1.000000 | 4.71e-07 |
| 15 | 1.178097 | 0.707107 | 0.707075 | 3.21e-05 |
| 20 | 1.570796 | 0.000000 | −0.000959 | 9.59e-04 |

Результаты вычисления второй производной y'' = 2cos(2x) при n = 4 (выборка) приведены в Таблице 2.2.

**Таблица 2.2 — Вторая производная y'' = 2cos(2x), полином Лагранжа степени n = 4 (выборка)**

| j | xⱼ | y''_точн | y''_полином | \|err\| |
|---|-----|---------|-----------|--------|
| 0 | 0.000000 | 2.000000 | 2.024275 | 2.43e-02 |
| 5 | 0.392699 | 1.414214 | 1.418078 | 3.86e-03 |
| 10 | 0.785398 | 0.000000 | 0.000000 | 4.32e-15 |
| 15 | 1.178097 | −1.414214 | −1.409129 | 5.08e-03 |
| 20 | 1.570796 | −2.000000 | −2.024275 | 2.43e-02 |

Сводная таблица максимальных погрешностей дифференцирования (n = 4) приведена в Таблице 2.3.

**Таблица 2.3 — Максимальные погрешности численного дифференцирования (n = 4)**

| Производная | max\|err\| |
|------------|-----------|
| y' | 9.59e-04 |
| y'' | 2.43e-02 |

*Примечание.* Погрешность второй производной заметно выше погрешности первой — дифференцирование усиливает ошибку интерполяции, а вблизи концов интервала точки [0, π/2] группа ближайших узлов асимметрична относительно xⱼ, что дополнительно увеличивает ошибку.

***

## 3 ЧИСЛЕННОЕ ИНТЕГРИРОВАНИЕ

### 3.1 Задание

1. Написать программу вычисления интеграла одним из методов (трапеций, Симпсона или прямоугольников) с автоматическим выбором шага интегрирования по правилу Рунге; требуемая точность задаётся пользователем. Выбрана **формула Симпсона**.
2. Написать программу вычисления интеграла по формуле Гаусса с заданной пользователем точностью.

**Вариант 10:**

$$I = \int_0^2 e^{-x^2} \cos x \, dx.$$

**Входные данные:** границы интегрирования [a, b] = [0, 2], требуемая точность ε (вводится пользователем отдельно для п. 3.1 и п. 3.2).

**Выходные данные:** значение интеграла I, число разбиений (или порядок формулы Гаусса), достигнутая оценка погрешности.

***

### 3.2 Теоретический материал

**Формула Симпсона:**

$$I \approx \frac{h}{3}\left[f(a) + 4f(x_1) + 2f(x_2) + 4f(x_3) + \ldots + 4f(x_{n-1}) + f(b)\right], \quad h = \frac{b-a}{n}.$$

Погрешность: $O(h^4)$, порядок точности p = 4. Требует чётного n.

**Правило Рунге** (автовыбор шага). Число разбиений удваивается (n → 2n) до выполнения условия

$$\frac{|I_{2n} - I_n|}{2^p - 1} < \varepsilon \cdot |I_{2n}|.$$

**Формула Гаусса–Лежандра.** Интеграл на [a, b] через узлы tₖ и веса wₖ квадратуры на [−1, 1]:

$$I \approx \frac{b-a}{2} \sum_{k=1}^n w_k \, f\!\left(\frac{a+b}{2} + \frac{b-a}{2} t_k\right).$$

Узлы tₖ — корни полинома Лежандра Pₙ(t) степени n, вычисляемые методом Ньютона:

$$t^{(m+1)} = t^{(m)} - \frac{P_n(t^{(m)})}{P_n'(t^{(m)})},$$

с начальным приближением $t_k^{(0)} = \cos\!\left(\pi \dfrac{k - 0{,}25}{n + 0{,}5}\right)$. Значения Pₙ и Pₙ' вычисляются трёхчленной рекуррентной формулой $k P_k(t) = (2k-1)tP_{k-1}(t) - (k-1)P_{k-2}(t)$. Веса вычисляются по формуле

$$w_k = \frac{2}{(1-t_k^2)\,[P_n'(t_k)]^2}.$$

Формула Гаусса–Лежандра из n узлов точна для полиномов степени ≤ 2n−1, что даёт быстрый рост точности с ростом n; порядок n подбирается программой автоматически — увеличивается на 1, пока результат не перестанет меняться в пределах заданной точности ε.

***

### 3.3 Порядок выполнения работы

**Формула Симпсона с правилом Рунге:**

1. Запросить у пользователя точность ε.
2. Задать начальное число интервалов n₀ = 4, вычислить I_n, затем I_{2n}.
3. Проверить критерий Рунге; при выполнении — результат I_{2n}, иначе n ← 2n и повторить.

**Формула Гаусса с заданной точностью:**

1. Запросить у пользователя точность ε.
2. Для текущего порядка n вычислить узлы и веса Гаусса–Лежандра (метод Ньютона для корней Pₙ), вычислить I_n.
3. Увеличить n на 1, вычислить I_{n+1}; если |I_{n+1} − I_n| / |I_{n+1}| < ε — результат I_{n+1}, иначе повторить шаг 3.

***

### 3.4 Результаты работы программы

Контрольное значение (формула Симпсона, n = 10 000): **I ≈ 0.6926114516**

Результат формулы Симпсона с правилом Рунге при ε = 10⁻⁶ приведён в Таблице 3.1.

**Таблица 3.1 — Результат формулы Симпсона с правилом Рунге (ε = 10⁻⁶)**

| Метод | I | n | Погрешность Рунге | \|I − I_ref\| |
|-------|---|---|-------------------|--------------|
| Симпсона | 0.6926108332 | 16 | 5.54e-07 | 6.18e-07 |

Результат формулы Гаусса–Лежандра с самостоятельно вычисленными узлами при ε = 10⁻⁶ приведён в Таблице 3.2.

**Таблица 3.2 — Результат формулы Гаусса–Лежандра с заданной точностью (ε = 10⁻⁶)**

| I | Порядок n | \|I − I_ref\| |
|---|-----------|--------------|
| 0.6926114486 | 8 | 3.06e-09 |

***

## 4 ВЫВОДЫ

**Задание 6.3 — Интерполяция.** Реализован полином Лагранжа со степенью, задаваемой пользователем: для каждой точки берутся n+1 ближайших узлов, что даёт равномерную по интервалу погрешность (при n = 4 — максимум 2.16·10⁻⁵) без характерного роста ошибки у границ, свойственного полиному с фиксированным направлением обхода узлов.

**Задание 6.4 — Численное дифференцирование.** Дифференцирование того же полинома Лагранжа даёт погрешность 9.59·10⁻⁴ для первой производной и 2.43·10⁻² для второй (при n = 4) — дифференцирование заметно усиливает погрешность интерполяции, особенно для второй производной и вблизи концов интервала, где группа ближайших узлов асимметрична.

**Задание 6.5 — Численное интегрирование.** Формула Симпсона с правилом Рунге достигла заданной точности ε = 10⁻⁶ при n = 16 разбиениях (фактическая погрешность 6.18·10⁻⁷). Формула Гаусса–Лежандра с самостоятельно вычисленными узлами и весами (метод Ньютона для корней полинома Лежандра) достигла точности 3.06·10⁻⁹ уже при порядке n = 8 — заметно меньшим числом вычислений подынтегральной функции, что подтверждает более высокую скорость сходимости квадратур Гаусса по сравнению с формулами Ньютона–Котеса того же порядка точности.

***

## 5 СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ

1. Мицель А. А., Романенко В. В. Вычислительная математика : методические указания по выполнению контрольной и лабораторных работ. — Томск : ФДО, ТУСУР, 2019. — 117 с.
2. Бахвалов Н. С., Жидков Н. П., Кобельков Г. М. Численные методы : учеб. пособие для вузов. — М. : БИНОМ. Лаборатория знаний, 2011. — 637 с.
3. Мицель А. А. Вычислительные методы : учеб. пособие. — Томск : В-Спектр, 2010. — 264 с.

***

## ПРИЛОЖЕНИЕ А (обязательное) — Листинги программ

### А.1 Задание 6.3 — Интерполяция (полином Лагранжа)

Файл: `lab2/task_6_3.py`

```python
"""
Задание 6.3 - Приближение функций (интерполяция).
Вариант 10: y = sin^2(x) + 1
Исходная сетка: xi = i*h,  h = pi/20,  i = 0,...,10
Новая сетка:   xj = j*h/2, j = 0,...,20

Реализован один метод - интерполяционный полином Лагранжа. Степень полинома
запрашивается у пользователя: для каждой точки новой сетки берутся n+1
ближайших узлов исходной сетки, и по ним строится полином Лагранжа степени n.
"""

import math

H = math.pi / 20
N_SRC = 10          # индексы исходных узлов 0..10
N_NEW = 20          # индексы новой сетки 0..20

x_src = [i * H for i in range(N_SRC + 1)]
y_src = [math.sin(xi) ** 2 + 1 for xi in x_src]

x_new = [j * H / 2 for j in range(N_NEW + 1)]
y_exact = [math.sin(xi) ** 2 + 1 for xi in x_new]


def read_degree():
    while True:
        raw = input(f"Введите степень интерполяционного полинома n (1..{N_SRC}): ")
        try:
            n = int(raw)
        except ValueError:
            print("  нужно целое число, попробуйте снова")
            continue
        if 1 <= n <= N_SRC:
            return n
        print(f"  n должно быть в диапазоне 1..{N_SRC}")


def nearest_nodes(x, x_nodes, count):
    """Возвращает индексы count узлов из x_nodes, ближайших к точке x (по возрастанию)."""
    indexed = list(range(len(x_nodes)))
    indexed.sort(key=lambda i: abs(x_nodes[i] - x))
    chosen = indexed[:count]
    chosen.sort()
    return chosen


def lagrange_value(x_nodes, y_nodes, x):
    n = len(x_nodes)
    total = 0.0
    for i in range(n):
        basis = 1.0
        for j in range(n):
            if j != i:
                basis *= (x - x_nodes[j]) / (x_nodes[i] - x_nodes[j])
        total += y_nodes[i] * basis
    return total


def interpolate(x_eval, degree):
    results = []
    for x in x_eval:
        idxs = nearest_nodes(x, x_src, degree + 1)
        nodes_x = [x_src[i] for i in idxs]
        nodes_y = [y_src[i] for i in idxs]
        results.append(lagrange_value(nodes_x, nodes_y, x))
    return results


def main():
    print("=" * 65)
    print("Задание 6.3. Вариант 10. Полином Лагранжа.")
    print("y = sin^2(x) + 1,  x in [0, pi/2],  h = pi/20")
    print("=" * 65)

    print("\nВходные данные - исходная сетка (xi, yi):")
    for xi, yi in zip(x_src, y_src):
        print(f"  x = {xi:.6f},  y = {yi:.8f}")

    degree = read_degree()
    print(f"\nСтепень полинома n = {degree} (используется {degree + 1} ближайших узлов на точку)")

    y_poly = interpolate(x_new, degree)

    print("\nВыходные данные - результаты интерполяции:")
    header = f"{'j':>3}  {'xj':>10}  {'y_точн':>12}  {'y_полином':>12}  {'|err|':>10}"
    print(header)
    max_err = 0.0
    for j in range(N_NEW + 1):
        xj = x_new[j]
        ye = y_exact[j]
        yp = y_poly[j]
        err = abs(ye - yp)
        max_err = max(max_err, err)
        print(f"{j:>3}  {xj:>10.6f}  {ye:>12.8f}  {yp:>12.8f}  {err:>10.2e}")

    print(f"\nМакс. погрешность интерполяции: {max_err:.2e}")


if __name__ == "__main__":
    main()
```

### А.2 Задание 6.4 — Численное дифференцирование (производные полинома Лагранжа)

Файл: `lab2/task_6_4.py`

```python
"""
Задание 6.4 - Численное дифференцирование.
Вариант 10: y = sin^2(x) + 1 (те же исходные данные, что и в задании 6.3).

Реализован один метод - дифференцирование интерполяционного полинома
Лагранжа. Степень полинома запрашивается у пользователя: для каждой точки
новой сетки берутся n+1 ближайших узлов исходной сетки, строится полином
Лагранжа степени n, и вычисляются его первая и вторая производные в точке.
"""

import math

H = math.pi / 20
N_SRC = 10
N_NEW = 20

x_src = [i * H for i in range(N_SRC + 1)]
y_src = [math.sin(xi) ** 2 + 1 for xi in x_src]
x_new = [j * H / 2 for j in range(N_NEW + 1)]


def dy_exact(x):
    return math.sin(2 * x)          # d/dx (sin^2 x + 1) = sin(2x)


def d2y_exact(x):
    return 2 * math.cos(2 * x)      # d^2/dx^2 (sin^2 x + 1) = 2*cos(2x)


def read_degree():
    while True:
        raw = input(f"Введите степень интерполяционного полинома n (2..{N_SRC}): ")
        try:
            n = int(raw)
        except ValueError:
            print("  нужно целое число, попробуйте снова")
            continue
        if 2 <= n <= N_SRC:
            return n
        print(f"  n должно быть в диапазоне 2..{N_SRC} (для второй производной нужно n >= 2)")


def nearest_nodes(x, x_nodes, count):
    indexed = list(range(len(x_nodes)))
    indexed.sort(key=lambda i: abs(x_nodes[i] - x))
    chosen = indexed[:count]
    chosen.sort()
    return chosen


def lagrange_derivatives_at(x_nodes, y_nodes, x):
    """Первая и вторая производная полинома Лагранжа, построенного по x_nodes/y_nodes, в точке x."""
    n = len(x_nodes)
    dy1 = 0.0
    dy2 = 0.0
    for i in range(n):
        dLi = 0.0
        d2Li = 0.0
        for k in range(n):
            if k == i:
                continue
            prod_k = 1.0
            for j in range(n):
                if j != i and j != k:
                    prod_k *= (x - x_nodes[j]) / (x_nodes[i] - x_nodes[j])
            dLi += prod_k / (x_nodes[i] - x_nodes[k])

            for m in range(n):
                if m == i or m == k:
                    continue
                prod_km = 1.0
                for j in range(n):
                    if j != i and j != k and j != m:
                        prod_km *= (x - x_nodes[j]) / (x_nodes[i] - x_nodes[j])
                d2Li += prod_km / ((x_nodes[i] - x_nodes[k]) * (x_nodes[i] - x_nodes[m]))
        dy1 += y_nodes[i] * dLi
        dy2 += y_nodes[i] * d2Li
    return dy1, dy2


def differentiate(x_eval, degree):
    dy1_list, dy2_list = [], []
    for x in x_eval:
        idxs = nearest_nodes(x, x_src, degree + 1)
        nodes_x = [x_src[i] for i in idxs]
        nodes_y = [y_src[i] for i in idxs]
        d1, d2 = lagrange_derivatives_at(nodes_x, nodes_y, x)
        dy1_list.append(d1)
        dy2_list.append(d2)
    return dy1_list, dy2_list


def main():
    print("=" * 75)
    print("Задание 6.4. Вариант 10. Дифференцирование полинома Лагранжа.")
    print("y = sin^2(x) + 1,  y' = sin(2x),  y'' = 2*cos(2x)")
    print("=" * 75)

    print("\nВходные данные - исходная сетка (xi, yi):")
    for xi, yi in zip(x_src, y_src):
        print(f"  x = {xi:.6f},  y = {yi:.8f}")

    degree = read_degree()
    print(f"\nСтепень полинома n = {degree} (используется {degree + 1} ближайших узлов на точку)")

    dy1, dy2 = differentiate(x_new, degree)

    print("\nВыходные данные - первая производная y':")
    header = f"{'j':>3}  {'xj':>10}  {'y_точн':>10}  {'y_полином':>10}  {'|err|':>8}"
    print(header)
    max_err1 = 0.0
    for j in range(N_NEW + 1):
        xj = x_new[j]
        exact = dy_exact(xj)
        err = abs(exact - dy1[j])
        max_err1 = max(max_err1, err)
        print(f"{j:>3}  {xj:>10.6f}  {exact:>10.6f}  {dy1[j]:>10.6f}  {err:>8.2e}")

    print("\nВыходные данные - вторая производная y'':")
    print(header)
    max_err2 = 0.0
    for j in range(N_NEW + 1):
        xj = x_new[j]
        exact = d2y_exact(xj)
        err = abs(exact - dy2[j])
        max_err2 = max(max_err2, err)
        print(f"{j:>3}  {xj:>10.6f}  {exact:>10.6f}  {dy2[j]:>10.6f}  {err:>8.2e}")

    print(f"\nМакс. погрешность y':  {max_err1:.2e}")
    print(f"Макс. погрешность y'': {max_err2:.2e}")


if __name__ == "__main__":
    main()
```

### А.3 Задание 6.5 — Численное интегрирование (Симпсон + Рунге, Гаусс-Лежандр)

Файл: `lab2/task_6_5.py`

```python
"""
Задание 6.5 - Численное интегрирование.
Вариант 10: I = int_0^2 e^(-x^2)*cos(x) dx

3.1 Квадратурная формула Симпсона с автоматическим выбором шага по правилу
    Рунге; точность запрашивается у пользователя.
3.2 Формула Гаусса-Лежандра: узлы и веса вычисляются самостоятельно (корни
    полинома Лежандра ищутся методом Ньютона), порядок формулы увеличивается
    до тех пор, пока результат не перестанет меняться в пределах заданной
    пользователем точности.
"""

import math

A, B = 0.0, 2.0


def f(x):
    return math.exp(-x ** 2) * math.cos(x)


def read_eps(prompt):
    while True:
        raw = input(prompt)
        try:
            eps = float(raw)
        except ValueError:
            print("  нужно число, попробуйте снова")
            continue
        if eps > 0.0:
            return eps
        print("  точность должна быть положительным числом")


# --- 3.1 Формула Симпсона с правилом Рунге ------------------------------

def simpson(a, b, n):
    if n % 2 != 0:
        n += 1
    h = (b - a) / n
    total = f(a) + f(b)
    for i in range(1, n):
        total += (4.0 if i % 2 != 0 else 2.0) * f(a + i * h)
    return total * h / 3.0


def simpson_with_runge(a, b, n0, eps):
    """Удваивает n, пока оценка погрешности Рунге не станет меньше eps. p = 4 для Симпсона."""
    p = 4
    n = n0
    i_prev = simpson(a, b, n)
    while True:
        n *= 2
        i_new = simpson(a, b, n)
        runge_error = abs(i_new - i_prev) / (2 ** p - 1)
        scale = abs(i_new) if abs(i_new) > 1e-15 else 1.0
        if runge_error / scale < eps:
            return i_new, n, runge_error
        i_prev = i_new


# --- 3.2 Формула Гаусса-Лежандра с собственным вычислением узлов --------

def legendre(n, x):
    """Значения P_n(x) и P_n'(x) через рекуррентное соотношение."""
    p_prev, p_curr = 1.0, x
    if n == 0:
        return 1.0, 0.0
    for k in range(2, n + 1):
        p_next = ((2 * k - 1) * x * p_curr - (k - 1) * p_prev) / k
        p_prev, p_curr = p_curr, p_next
    derivative = n * (x * p_curr - p_prev) / (x * x - 1.0)
    return p_curr, derivative


def legendre_nodes_weights(n):
    """Узлы и веса формулы Гаусса-Лежандра на [-1, 1], узлы уточняются методом Ньютона."""
    nodes = []
    weights = []
    for i in range(n):
        x = math.cos(math.pi * (i + 0.75) / (n + 0.5))
        for _ in range(100):
            value, derivative = legendre(n, x)
            step = value / derivative
            x -= step
            if abs(step) < 1e-15:
                break
        _, derivative = legendre(n, x)
        w = 2.0 / ((1.0 - x * x) * derivative * derivative)
        nodes.append(x)
        weights.append(w)
    order = sorted(range(n), key=lambda k: nodes[k])
    return [nodes[k] for k in order], [weights[k] for k in order]


def gauss_integrate(a, b, n):
    nodes, weights = legendre_nodes_weights(n)
    mid = (a + b) / 2.0
    half = (b - a) / 2.0
    total = 0.0
    for t, w in zip(nodes, weights):
        total += w * f(mid + half * t)
    return total * half


def gauss_with_precision(a, b, eps, n_start=2, n_max=40):
    """Увеличивает порядок формулы Гаусса, пока результат не стабилизируется с точностью eps."""
    n = n_start
    i_prev = gauss_integrate(a, b, n)
    while n < n_max:
        n += 1
        i_new = gauss_integrate(a, b, n)
        scale = abs(i_new) if abs(i_new) > 1e-15 else 1.0
        if abs(i_new - i_prev) / scale < eps:
            return i_new, n
        i_prev = i_new
    return i_prev, n


def main():
    print("=" * 60)
    print("Задание 6.5. Вариант 10.")
    print("I = int_0^2 e^(-x^2)*cos(x) dx")
    print("=" * 60)

    i_ref = simpson(A, B, 10000)
    print(f"\nКонтрольное значение (Симпсон, n=10000): I = {i_ref:.10f}")

    print("\n--- 3.1 Формула Симпсона с правилом Рунге ---")
    eps1 = read_eps("Введите точность интегрирования eps: ")
    i_simp, n_simp, runge_err = simpson_with_runge(A, B, 4, eps1)
    print(f"\nВыходные данные:")
    print(f"  I = {i_simp:.10f}")
    print(f"  число разбиений n = {n_simp}")
    print(f"  оценка погрешности по Рунге = {runge_err:.2e}")
    print(f"  |I - I_ref| = {abs(i_simp - i_ref):.2e}")

    print("\n--- 3.2 Формула Гаусса-Лежандра с заданной точностью ---")
    eps2 = read_eps("Введите точность вычисления интеграла eps: ")
    i_gauss, n_gauss = gauss_with_precision(A, B, eps2)
    print(f"\nВыходные данные:")
    print(f"  I = {i_gauss:.10f}")
    print(f"  порядок формулы (число узлов) n = {n_gauss}")
    print(f"  |I - I_ref| = {abs(i_gauss - i_ref):.2e}")


if __name__ == "__main__":
    main()
```
