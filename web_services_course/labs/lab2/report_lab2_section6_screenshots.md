<a name="6"></a>
## 6 Результаты работы программы

После реализации основных маршрутов выполнена проверка работы REST API на тестовом наборе данных. Ниже приведены скриншоты запросов и ответов для каждого метода (рисунки 6.1–6.9).

Запрос `GET /tracks` без параметров возвращает список всех произведений (рисунок 6.1).

*[скриншот сюда]*

Рисунок 6.1 — GET /tracks (список произведений без сортировки)

---

При передаче параметров `sort_by=year` и `order=desc` та же коллекция сортируется по году выпуска в порядке убывания (рисунок 6.2).

*[скриншот сюда]*

Рисунок 6.2 — GET /tracks?sort_by=year&order=desc (сортировка по году убывания)

---

Маршрут `GET /tracks/stats` возвращает агрегаты (`avg`, `max`, `min`) по числовым полям `duration_sec`, `year`, `rating` (рисунок 6.3).

*[скриншот сюда]*

Рисунок 6.3 — GET /tracks/stats (агрегаты по числовым полям)

---

Запрос `POST /tracks` с телом `{"title":"Test","artist":"A","genre":"G","duration_sec":200,"year":2020,"rating":4.0}` создаёт новую запись — ответ `201` с присвоенным `id: 4` (рисунок 6.4). На скриншоте желательно показать и тело запроса, и ответ.

*[скриншот сюда]*

Рисунок 6.4 — POST /tracks (создание записи)

---

Запрос `PUT /tracks/1` с телом `{"rating": 5.0}` обновляет только поле `rating`, остальные поля записи сохраняются — ответ `200` (рисунок 6.5).

*[скриншот сюда]*

Рисунок 6.5 — PUT /tracks/1 (частичное обновление)

---

Запрос `DELETE /tracks/4` удаляет запись — ответ `204` без тела (рисунок 6.6).

*[скриншот сюда]*

Рисунок 6.6 — DELETE /tracks/4 (удаление записи)

---

Запрос `GET /tracks/999` к несуществующему id возвращает `404` с телом `{"error": "track not found"}` (рисунок 6.7).

*[скриншот сюда]*

Рисунок 6.7 — GET /tracks/999 (несуществующий id, 404)

---

Запрос `POST /tracks` с неполным телом `{"title":"x"}` возвращает `400` со списком ошибок валидации: `artist is required`, `genre is required`, `duration_sec is required`, `year is required`, `rating is required` (рисунок 6.8).

*[скриншот сюда]*

Рисунок 6.8 — POST /tracks с неполным телом (400, ошибки валидации)

---

Документация API доступна автоматически по адресу `/apidocs/` — на рисунке 6.9 показан обзор всех задокументированных маршрутов.

*[скриншот сюда]*

Рисунок 6.9 — Swagger UI (`/apidocs/`) со списком эндпоинтов
